<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
	http://www.springframework.org/schema/context
    http://www.springframework.org/schema/context/spring-context-4.0.xsd
    http://www.springframework.org/schema/mvc
    http://www.springframework.org/schema/mvc/spring-mvc-4.0.xsd
    http://www.springframework.org/schema/aop
    http://www.springframework.org/schema/aop/spring-aop.xsd">

    <!-- 注解扫描 -->
    <context:component-scan base-package="com.ctp.ghub.controller"/>

    <!-- 静态资源(js、image等)的访问 -->
    <mvc:default-servlet-handler/>

    <!-- 启动对@AspectJ注解的支持 proxy-target-class设置为true表示通知spring使用
    cglib而不是jdk的来生成代理方法，这样AOP可以拦截到Controller -->
    <aop:aspectj-autoproxy proxy-target-class="true"/>
    <!-- 这里要手动配置controllerAop bean ,因为上面的扫描只扫描到com.ctp.ghub.controller 目录 -->
    <bean id="controllerAop" class="com.ctp.ghub.aop.ControllerAop"/>

    <!-- 使用fastjson 配置 ,支持转化的请求数据类型为三种 -->
    <!-- mvc:annotation-driven 会自动注册RequestMappingHandlerMapping与
    RequestMappingHandlerAdapter两个Bean,这是Spring MVC为@Controller分发请求所必需的 -->
    <mvc:annotation-driven>
        <mvc:message-converters>
            <bean class="com.alibaba.fastjson.support.spring.FastJsonHttpMessageConverter">
                <property name="supportedMediaTypes">
                    <list>
                        <value>text/html;charset=UTF-8</value>
                        <value>application/json</value>
                        <value>application/xml;charset=UTF-8</value>
                    </list>
                </property>
                <property name="features">
                    <list>
                        <!-- 默认的意思就是不配置这个属性 -->
                        <!-- 是否输出值为null的字段 ，默认是false-->
                        <value>WriteMapNullValue</value>
                        <value>WriteNullNumberAsZero</value>
                        <value>WriteNullListAsEmpty</value>
                        <value>WriteNullStringAsEmpty</value>
                        <value>WriteNullBooleanAsFalse</value>
                        <value>WriteDateUseDateFormat</value>
                    </list>
                </property>
            </bean>
        </mvc:message-converters>
    </mvc:annotation-driven>

    <!-- 配置页面渲染 -->
    <!--<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">-->
        <!--<property name="prefix" value="/WEB-INF/views/"/>-->
        <!--<property name="suffix" value=".jsp"/>-->
    <!--</bean>-->

    <!-- 配置VelocityConfigurer -->
    <bean id="velocityConfigurer" class="org.springframework.web.servlet.view.velocity.VelocityConfigurer">
        <!-- 模板存放的路径 -->
        <property name="resourceLoaderPath" value="/WEB-INF/velocity/templates"/>
        <!-- 模板设置参数路径 -->
        <property name="configLocation" value="classpath:velocity/velocity.properties"/>
    </bean>

    <!--配置试图解析器-->
    <bean id="viewResolver" class="org.springframework.web.servlet.view.velocity.VelocityLayoutViewResolver">
        <property name="cache" value="false"/>
        <!-- 视图文件的前缀，即存放的路径 -->
        <property name="prefix" value="/"/>
        <!-- 视图文件的后缀名 -->
        <property name="suffix" value=".vm"/>
        <!--指定编码-->
        <property name="contentType" value="text/html;charset=utf-8"/>
        <!--toolbox配置文件路径-->
        <property name="toolboxConfigLocation" value="/WEB-INF/toolbox.xml"/>
        <!--指定layout文件-->
        <property name="layoutUrl" value="/WEB-INF/layout/layout.vm" />
        <!-- 设定模板文件键值，设定该值后就可以在vm文件中使用该键值设置模板路径 -->
        <property name="layoutKey" value="layout"/>
        <!-- 表示指定vm文件显示位置 -->
        <property name="screenContentKey" value="screen_content" />
        <!--是否使用spring对宏定义的支持-->
        <property name="exposeSpringMacroHelpers" value="true"/>
        <!--是否开放session属性-->
        <property name="exposeSessionAttributes" value="true"/>
        <property name="allowSessionOverride" value="true"/>
        <property name="allowRequestOverride" value="true"/>
        <!--是否开放request属性-->
        <property name="exposeRequestAttributes" value="true"/>
        <!--request属性引用名称-->
        <property name="requestContextAttribute" value="rc"/>
        <property name="dateToolAttribute">
            <value>dateTool</value> <!--日期函数名称-->
        </property>
        <property name="numberToolAttribute">
            <value>numberTool</value> <!--数字函数名称-->
        </property>

    </bean>

    <!-- 配置全局异常处理器 -->
    <bean id="exceptionResolver" class="com.ctp.ghub.exception.SpringGlobalExceptionHandler"></bean>
</beans>